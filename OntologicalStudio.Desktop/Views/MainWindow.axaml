<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="using:OntologicalStudio.Desktop.ViewModels"
        xmlns:views="using:OntologicalStudio.Desktop.Views"
        mc:Ignorable="d" d:DesignWidth="1200" d:DesignHeight="800"
        x:Class="OntologicalStudio.Desktop.Views.MainWindow"
        x:DataType="vm:MainWindowViewModel"
        Title="Ontological Studio"
        Width="1200" Height="800"
        WindowStartupLocation="CenterScreen">

    <Grid RowDefinitions="Auto,*">
        <Border Grid.Row="0" Background="#1f1f23" Padding="16,10">
            <Grid ColumnDefinitions="*,Auto" VerticalAlignment="Center">
                <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="12">
                    <TextBlock Text="{Binding AppTitle}" FontSize="18" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding AppSubtitle}" FontSize="12" Opacity="0.6" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="6">
                    <TextBlock Text="{Binding LanguageLabel}" VerticalAlignment="Center" Opacity="0.75"/>
                    <ComboBox Width="90"
                              ItemsSource="{Binding AvailableLanguages}"
                              SelectedValueBinding="{Binding Code}"
                              SelectedValue="{Binding SelectedLanguageCode, Mode=TwoWay}">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Label}"/>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                </StackPanel>
            </Grid>
        </Border>

        <TabControl Grid.Row="1" Margin="8" SelectedIndex="{Binding SelectedTabIndex, Mode=TwoWay}">
            <TabItem Header="{Binding UniversesTabHeader}">
                <ContentControl Content="{Binding Universes}">
                    <ContentControl.ContentTemplate>
                        <DataTemplate>
                            <views:UniversesView DataContext="{Binding}"/>
                        </DataTemplate>
                    </ContentControl.ContentTemplate>
                </ContentControl>
            </TabItem>
            <TabItem Header="{Binding CanvasTabHeader}">
                <ContentControl Content="{Binding UniverseCanvas}">
                    <ContentControl.ContentTemplate>
                        <DataTemplate>
                            <views:UniverseCanvasView DataContext="{Binding}"/>
                        </DataTemplate>
                    </ContentControl.ContentTemplate>
                </ContentControl>
            </TabItem>
            <TabItem Header="{Binding EntitiesTabHeader}">
                <ContentControl Content="{Binding Entities}">
                    <ContentControl.ContentTemplate>
                        <DataTemplate>
                            <views:EntitiesView DataContext="{Binding}"/>
                        </DataTemplate>
                    </ContentControl.ContentTemplate>
                </ContentControl>
            </TabItem>
            <TabItem Header="{Binding RelationshipsTabHeader}">
                <ContentControl Content="{Binding Relationships}">
                    <ContentControl.ContentTemplate>
                        <DataTemplate>
                            <views:RelationshipsView DataContext="{Binding}"/>
                        </DataTemplate>
                    </ContentControl.ContentTemplate>
                </ContentControl>
            </TabItem>
            <TabItem Header="{Binding ScenariosTabHeader}">
                <ContentControl Content="{Binding Scenarios}">
                    <ContentControl.ContentTemplate>
                        <DataTemplate>
                            <views:ScenariosView DataContext="{Binding}"/>
                        </DataTemplate>
                    </ContentControl.ContentTemplate>
                </ContentControl>
            </TabItem>
            <TabItem Header="{Binding PromptPreviewTabHeader}">
                <ContentControl Content="{Binding Prompt}">
                    <ContentControl.ContentTemplate>
                        <DataTemplate>
                            <views:PromptPreviewView DataContext="{Binding}"/>
                        </DataTemplate>
                    </ContentControl.ContentTemplate>
                </ContentControl>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
